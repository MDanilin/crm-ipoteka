# Port the Fastify API listens on
PORT=3001

# JWT / session secret (unused, using simple token in DB currently)
# SECRET=change-me-in-production

# Database path (defaults to apps/api/crm.db)
# DB_PATH=./crm.db
